# Development Log - 2026-04-22

## Project Goal

Build a Zotero plugin that detects figures and tables in PDF attachments,
shows them in the reader side pane, supports quick navigation to their PDF
locations, and later supports enlarged preview of clicked figure/table regions.

## Completed Today

### Stage 1: Template Baseline

- Installed dependencies with `npm install`.
- Confirmed the Zotero plugin template builds successfully.
- Verified the build pipeline with:

```powershell
npm run build
```

### Stage 2: Template Cleanup and Plugin Identity

- Renamed the project from the template to `Zotero Fig`.
- Updated package metadata:
  - package name: `zotero-fig`
  - version: `0.1.0`
  - addon name: `Zotero Fig`
  - addon ID: `zoterofig@forgeters.github.io`
  - addon ref: `zoterofig`
  - addon instance: `ZoteroFig`
- Removed template demo code:
  - sample menus
  - shortcuts
  - dialogs
  - preference pane
  - demo styles
  - old template docs
- Replaced `src/hooks.ts` with a clean lifecycle skeleton.
- Updated README for the new plugin.

### Stage 3: Reader Figure Panel MVP

- Added `src/modules/figurePanel.ts`.
- Registered a Zotero reader item-pane section with:
  - title: `Figures and Tables` / `图表`
  - section summary count
  - refresh button
- Added three fake figure/table entries:
  - `Figure 1` -> page 1
  - `Table 1` -> page 2
  - `Figure 2` -> page 3
- Implemented click navigation via `reader.navigate({ pageIndex })`.
- Fixed reader panel layout:
  - replaced native `button` with `div role="button"`
  - added stable spacing and minimum height
  - fixed caption text being hidden/overlapped
- Updated Zotero compatibility in `addon/manifest.json`:
  - `strict_min_version`: `7.0`
  - `strict_max_version`: `9.*`
- Confirmed manual XPI installation works in Zotero 9.0.1.

## Validation Results

The following commands passed:

```powershell
npm run lint:check
npm run build
```

The generated XPI is:

```text
.scaffold/build/zotero-fig.xpi
```

Manual testing completed:

- `npm start` launches Zotero with the plugin.
- Reader side pane shows the `图表` section.
- The three fake entries display with readable spacing.
- Clicking entries jumps to PDF pages 1, 2, and 3.
- The XPI installs successfully in Zotero 9.0.1.
- Zotero 9 incompatibility warning was fixed by expanding `strict_max_version`
  to `9.*`.

## Current Important Files

- `src/hooks.ts`: plugin lifecycle registration.
- `src/modules/figurePanel.ts`: reader side panel MVP.
- `addon/manifest.json`: Zotero compatibility and addon metadata template.
- `addon/locale/en-US/mainWindow.ftl`: English reader panel labels.
- `addon/locale/zh-CN/mainWindow.ftl`: Chinese reader panel labels.
- `README.md`: project overview and development commands.

## Next Stage

Stage 4 should replace the fake entries with real PDF caption detection.

Recommended implementation path:

1. Get the active Zotero reader instance.
2. Access PDF.js document/page text content from the reader.
3. Scan page text for figure/table captions with patterns such as:
   - `Figure 1`
   - `Fig. 1`
   - `Table 1`
   - `图 1`
   - `表 1`
4. Convert matches into the same `FigureEntry` structure currently used by the
   fake list.
5. Keep `reader.navigate({ pageIndex })` as the first navigation behavior.
6. Only after caption detection works, start extracting thumbnails or bounding
   boxes.

## Notes

- The current branch has uncommitted local changes. Review with:

```powershell
git status --short
```

- Do not run `npm audit fix --force` yet. The scaffold has known dependency
  warnings, and force-fixing may break template tooling.
